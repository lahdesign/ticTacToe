//create a blank board

let board = [
  ['', '', '']
  ['', '', '']
  ['', '', '']
]

let CurrentPlayer=

// a function that draws a board
let drawBoard = []
let gameBoard = []

let row0 = []
let row1 = []
let row2 = []

for(let i = 0; tray.length; i++) {
  if(rowCounter>= 0 && <= 2) {
    // on row 0
  } else if(i > 2 && i <=6) {
    const row1 = tray[i]
    // on row 1
  }
} else if(i > 6 && i <=9) {
  const row2 = tray[i]
  // on row 2
}
}

for (let column = 0; column < boardColumns.length; column++){
for(let row = 0; row < boardRows.lenght; row++){
//TODO may need if clause to check which column/ row we are on

if (column === 0) {

} else if (column === 1) {

}
    gameBoard.push(allboard[column][row])   // 0,0 poisition
}
}
